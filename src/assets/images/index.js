const images = {
    logo: require('../images/logo.svg').default, // File svg thì phải chấm . default
    imagesDefault: require('../images/imagesDefault.png'), // file png, jp,l... thif k cần chấm default
};

export default images;
